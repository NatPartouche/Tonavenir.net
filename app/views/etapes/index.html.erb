<h1>Listing etapes</h1>

<table>
  <tr>
    <th>Titre</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @etapes.each do |etape| %>
  <tr>
    <td><%= etape.titre %></td>
    <td><%= etape.description %></td>
    <td><%= link_to 'Show', etape %></td>
    <td><%= link_to 'Edit', edit_etape_path(etape) %></td>
    <td><%= link_to 'Destroy', etape, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Etape', new_etape_path %>
